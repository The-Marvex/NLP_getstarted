#include<iostream>
#include<string.h>
#include<fstream>
#include<vector>

using namespace std;
int main()
{
	int x = 0;
	int y = 0;
	if(x==(y+1)) cout<<"sanjeev";
	if(y==1) cout<<"mahima";
	else if (x == 0) cout<<"pankja";
	else if (y ==0) cout<<"vedant";
	else cout<<"raghav";
}